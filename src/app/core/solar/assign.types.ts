export interface Assign{
    id: string
    insertList: [StationList, StationListName]
}
export interface StationList{
    station:string[]
}

export interface StationListName{
    stationname:string[]
}

